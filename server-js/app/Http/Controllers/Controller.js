// Base Controller class
// This is the abstract base controller that all other controllers can extend from
// Currently empty but can be used for shared controller functionality

export default class Controller {
  // Base controller methods can go here
}
