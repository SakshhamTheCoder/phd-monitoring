import React from 'react';
import FormList from '../../forms/formList/FormList';

/** A scholar's own forms of one type, as cards rather than a table. */
const FormCardsBlock = () => <FormList />;

export default FormCardsBlock;
